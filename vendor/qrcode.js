// PLACEHOLDER - not the real library.
// A QR GENERATOR (e.g. davidshimjs/qrcodejs). The scanner vendors jsQR, which only DECODES;
// the student pass page needs the opposite direction, so this is a second and separate
// dependency. Drop your copy here before publishing docs/, or pass.html renders an empty
// white box on a dynamic day and nobody can be scanned.
